#pr4. Write a Python program which accepts the radius of a circle from the user and compute the area.
from math import pi
r = float(input("Enter the radius: "))
print(f"The radius of circle is {pi*r**2}")